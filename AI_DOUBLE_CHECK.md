# AI 個股評分共用雙重驗證流程

## 啟動條件

使用者輸入「開始選股」後，立即執行本流程，不要再要求使用者貼上其他提示詞。此觸發詞適用於 Gemini、ChatGPT 及其他能讀取本專案文件的 AI。

## 共同分析規則

- 兩次分析都必須完整遵守 `AI_SCORING_RULES.md`，不得使用各 AI 自己的另一套評分標準。
- 分析 `reports/` 內全部最新個股報表，使用相同原始資料與截止日期。
- 固定檢查價格結構、MACD/KD、三大法人逐日方向、融資、防禦牆與 K 線型態。
- 集保只當背景，不得計分。
- 台股配色固定為紅漲綠跌。

## 第一次分析

1. 依 `AI_SCORING_RULES.md` 逐檔完成全面分析與綜合分數。
2. 寫入 `data.js`，保留分析日期、分數、決策、型態、多空證據與原始摘要。
3. 依綜合分數降冪取 TOP 5；同分依股票代號排序。

## 第二次獨立分析

1. 使用隔離的新工作階段或子代理重新分析全部報表。
2. 第二次只能讀取 `AI_SCORING_RULES.md` 與同批 `reports/`。
3. 第二次禁止讀取 `data.js` 的 winRate/決策、`ai_rankings.json`、現有排行與第一次輸出。
4. 獨立輸出 TOP 5，每筆必須包含 `rank`、`code`、`name`、`score`、`decision` 與附具體數字的 `reason`。

## 一致性比對

- 第二次完成後，才能與第一次 TOP 5 比對。
- 五檔股票、名次與分數全部相同：POST 到 `/api/ai-rankings/upsert`，標示「分析吻合」。
- 任一不同：不儲存、不得強制改成第一次答案；列出矛盾後，使用同一份 `AI_SCORING_RULES.md` 啟動新的獨立重判。
- 重判後仍不一致：停止建立有效榜單，標示「分析矛盾尚未解除」，交由使用者決定。

## 完成回報

簡要回報分析日期、第一次 TOP 5、第二次 TOP 5、是否吻合、矛盾項目及是否需要人工判斷。
