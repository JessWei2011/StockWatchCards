# StockWatchCards — AI 個股分析卡片

這個目錄是一個純前端小網站（`index.html` + `style.css` + `render.js` + `data.js`），
透過 GitHub Pages 發布，讓使用者在任何電腦、任何瀏覽器都能看到同一份「個股 AI 分析卡片」。

Repo: https://github.com/JessWei2011/StockWatchCards

## 使用者的核心工作流程

使用者會把 AI（另一套分析工具）產生的個股分析結論，直接貼到這個對話裡。
格式通常長這樣（欄位順序可能略有不同，但核心資訊固定）：

```
【個股】股票代號 股票名稱 【收盤價】xxx 【決策】加碼/進場/續抱觀望/減碼/出場 | 勝率評估：xx%
【型態辨識】xxx型態 + 信心度：高/中/低

* 基本面觀察： ...
* 技術數據面： ...
* 籌碼面解析： ...
* 訊號面判定： ...
* 支持進場證據： ...
* 反對進場證據： ...
* 最終持股建議： ...
```

看到這種貼上的文字時，直接照下面規則處理，不用特別詢問使用者「要不要幫你做成卡片」。

## data.js 卡片欄位規範

`STOCK_CARDS` 是陣列，每檔股票一筆物件。欄位：

```js
{
  code: "3034",              // 股票代號
  name: "聯詠",               // 股票名稱
  date: "2026-08-05",        // 分析日期（YYYY-MM-DD，用原文提到的日期或當天日期）
  group: "IC設計",            // 產業分類，見下方「自動分組規則」
  decision: "加碼",           // 原文的決策用詞，原封不動照抄（加碼/進場/續抱觀望/減碼/出場…）
  winRate: 75,                // 數字，不要加 % 符號
  pattern: "N字波段強勢突破箱型與布林上軌",  // 型態辨識，濃縮但保留關鍵字
  confidence: "高",           // 信心度
  current: "550.0",           // 收盤價
  entry: "520.60",            // 買進/短線防守參考價（通常是原文的短線移動止損位，例如 MA5）
                               // 若決策是「續抱觀望」且原文沒有建議新進場價，這裡留空字串 ""
  resist: "606.0",            // 壓力／目標價（52週高點、布林上軌、下一個關卡等）
  stop: "497.05",             // 較嚴格的停損／出場價（原文的「全面出場」防守價）
  bullish: [ /* 最多列 3-4 條，原文「支持進場證據」濃縮 */ ],
  bearish: [ /* 同上，取自「反對進場證據」 */ ],
  action: "一段濃縮過的行動方針，把「最終持股建議」壓成 2-3 句話",
  raw: `完整貼上的原文，一字不改，翻卡背面時完整顯示`
}
```

## 規則

1. **不保留歷史**：同一檔股票（比對 `code`）如果使用者又貼一次新分析，直接覆蓋整個物件（含 raw），
   不要新增第二筆、也不要保留舊資料的痕跡。
2. **完全重複偵測**：貼上的內容如果跟現有某張卡片的 `raw` 幾乎一模一樣（同代號、同數據），
   視為使用者複製貼上重複，跳過不處理，並告知使用者「這則已經存在，略過」。
3. **自動分組**：新股票第一次出現時，用 WebSearch 查詢「{代號} {名稱} 產業類別」，
   依台股官方產業分類（例如 IC設計、電腦及週邊設備、電子零組件-PCB/載板等）填入 `group`。
   同產業的股票盡量用同一個 `group` 字串，方便前端自動歸類在一起。
   使用者也可能直接說「把 XXX 放進 YYY 分組」，照使用者指定的優先。
4. **決策分色**：前端 `render.js` 的 `decisionClass()` 會依關鍵字判斷顏色 —
   買進類（🟢綠）：加碼/買進/買入/進場；賣出類（🔴紅）：減碼/賣出/出場；其餘（🔵藍）：持有/續抱觀望等。
   不需要額外欄位控制顏色，`decision` 文字本身就會決定。
5. **每組內排序**：卡片會自動依 `winRate` 由高到低排序（`render.js` 的 `groupAndSort()` 已處理），不用手動調整順序。
6. **完成後動作**：
   - `node -c data.js` 確認語法正確
   - 修改 `index.html` 裡 `style.css?v=N` / `data.js?v=N` / `render.js?v=N` 的版本號 +1（三個一起改，避免瀏覽器快取舊檔案）
   - `git add` 變更的檔案（只會是 `data.js` / `index.html`，除非同時改了樣式或邏輯）
   - `git commit` 一則簡短說明這批新增/更新了哪些股票
   - `git push`
7. **一次可以貼多筆**：使用者可能一次貼多檔分析（用空行或另一個 `【個股】` 開頭分隔），
   全部處理完再一次 commit + push，不用每筆都個別詢問確認。
8. **移除卡片**：使用者若明確說「移除／刪除某股票」，要真的從 `data.js` 的 `STOCK_CARDS` 陣列拿掉整個物件並 push
   （前端網頁上的 ✕ 按鈕只是「本機隱藏」，不會動到 `data.js`，跟這裡的「移除」是兩回事）。

## 自行從原始數據做分析（不是使用者貼現成文字時）

使用者的 `台X資訊.py` 會在這個資料夾產生每檔股票的 `{代號}_{名稱}(TW/TWO).html` + `{代號}_{名稱}(TW/TWO)_chart.png`，
內含完整原始數據（基本資訊、技術指標、近50日K線逐日數據、三大法人近20日、融資融券近20日、大戶持股週報）。

如果使用者要求「自己讀數據做分析」而不是貼別人分析好的文字，**必須嚴格套用以下規則**（這是使用者實際使用的 Gemini
分析工具的 gem 設定，逐字對齊過，不要自行發明別的判斷邏輯）：

### 角色與輸出格式
- 定位：專注台股短中波段交易的量化分析師，極度謹慎、只依數據判斷，不做預測性樂觀發言，也不因保守而看空。
- 最終輸出固定格式：
  ```
  【個股】股票名稱 代號 【收盤價】xxx
  【決策】進場 / 加碼 / 續抱觀望 / 減碼 / 出場（五選一） | 勝率評估：XX%
  【型態辨識】[型態名稱] + 信心度：[高/中/低]
  ```
  接著依序輸出 7 段（子彈筆記格式）：基本面觀察／技術數據面／籌碼面解析／訊號面判定／支持進場證據／反對進場證據／最終持股建議。

### 圖片判讀規則
- 有 `_chart.png` 時要打開來看，型態判斷（M頭/W底/N字反攻/箱型整理等）以**圖片視覺**為主，文字數據只是交叉驗證。
- 台股慣例：紅色=上漲、綠色=下跌，不要看反。
- 型態辨識務必附「辨識正確率」：高（圖形明顯+量能確認）／中（圖形類似但量能證據不足）／低（僅局部相似，需註明「圖形不夠清楚，僅供參考」）。
- 起跌訊號判斷順序：跌破圖上前波谷 > MACD死叉+柱狀轉負 > K跌破D且兩者皆<50 > 大戶籌碼轉賣。至少 2 個條件同時滿足才算訊號成立，並具體指出觸發項目。

### 核心決策規則 — 邊際動能優先（最重要）
拒絕落後指標陷阱，不做 20 日/月級別的平均趨勢盲從，重點聚焦「近 1~3 個交易日的邊際變化」與「最新一週籌碼的極端流向」。

三大構面：
1. **價格結構**：近 1~2 日收盤是否帶量站上/跌破布林中軌（20MA），通道開口是否擴張發散。
2. **動能加速度**：MACD 柱狀圖（Hist）近 1~3 日的加速度（顯著縮腳或連續翻正），不是慢速 DIF/Signal 零軸交叉；KD/RSI 看近 1~3 日邊際開口方向與角度，不是絕對數值。
3. **籌碼邊際流向**：三大法人近 1~3 日連續買賣超為主要依據（20 日累計僅供長線參考，不可因 20 日累計為負而忽視近期強勢回補）；週集保大戶要「逐級比較」400/600/800/1000 張與前一週的差異，若最新一週千張大戶單週變動 >0.5%（回補或暴撤），**該項要給最高權重**；散戶(<400張)是反向指標，比例下降+量價齊揚偏多，比例快速上升偏空。

**判定門檻：三大構面中，只要「至少兩項」同時在近 1~3 日轉強，就可以給出進場/加碼（或減碼/出場）等方向性決策 —— 不需要三項全部一致。只有單一構面轉折時，一律判定為「續抱觀望」。**

若籌碼資料少於 3 週，要誠實標註「資料週數過少，趨勢僅供參考」。

### 當日異常量價的整體判斷（否決條件，優先於兩項轉強門檻，用判斷力而非套公式）
即使三大構面中有兩項達到轉強門檻，仍必須先用整體判斷力檢視**最新一日**的K線／量能／籌碼是否透露「出貨／派發／急殺」的味道；
一旦判斷成立，直接否決進場/加碼，改判「續抱觀望」（原本偏空的訊號則維持或加重減碼/出場）。

這不是查表對答案，而是綜合以下幾類徵兆做整體判斷（沒有固定門檻數字，數字只是輔助參考起點，實際要結合K線圖形、
與近期常態的落差程度、多個訊號是否同時出現來下結論；單一項目輕微異常不必然否決，多個項目同時出現異常味道時要提高警覺）：

- **量能與收盤位置的落差**：當日量是否明顯脫離近期常態（不是死守1.5倍這種硬門檻，而是看「量能跳空放大到不尋常的程度」），
  且收盤位置是否顯示賣壓在盤中壓回（收在當日區間偏低處、留長上影線、或開高走低），兩者同時出現時警覺度要提高。
- **籌碼流向的方向反轉**：法人（尤其外資）是否從近期的買超態勢，在最新一天出現明顯反向轉折——不只看今天單日金額大小，
  也要看這個轉折跟前幾日的動能是否不成比例（例如前面買得溫和、今天卻賣得又急又猛，或反之）。
- **技術指標的極端與背離**：KDJ、RSI 等短線指標是否來到歷史上少見的極端值，且價格創高但指標未能同步創高（背離），
  這種「價創新高、指標卻走弱」的組合本身就是警訊，不需要卡在某個絕對數字才算數。

這條規則的核心精神：**不要只看近1~3日的方向性總和，最新一天的量價行為本身可能就是獨立訊號**，
用途是避免像「均線多頭排列+MACD轉強」這類落後或持續性指標掩蓋掉當天發生的反轉徵兆。
判斷時可以參照下方「案例庫」裡實際驗證過的具體案例做類比，而不是只套用抽象原則。

### 案例庫（持續累積，用來校準判斷力，不是規則本身）
每次使用者對某次分析的決策提出糾正或明確認可，且理由具有可類推性時，就在這裡新增一筆案例摘要
（股票代號、日期、當時數據、原判斷、修正後判斷、關鍵理由）。做新分析時，先看這裡有沒有相似情境可以類比參照。

- **2026-08-07　3693 營邦**：均線多頭排列(MA60>5>20>10)、MACD Hist持續擴大，若機械套用「兩項轉強即可進場」會判進場。
  但當日爆量2.5M（近期均量0.6~1.2M）、開549高562低522收527（收盤僅略高於當日低點）、法人由連續買超轉當日賣超、
  KDJ的J值98.7嚴重超買——多個異常同時出現，判斷應為「疑似出貨」，改判續抱觀望。
  **理由**：均線與MACD是落後/持續性指標，容易掩蓋當天發生的反轉徵兆；當量價與籌碼的「當日轉折」訊號夠強夠多時，
  應該否決掉由落後指標支撐的進場判斷。

### 證據與用詞原則
- 僅限用上傳的圖文資料，不引入外部新聞、歷史記憶或腦補未來展望。
- 每個判斷句要附具體數字或圖上位置（例：「收盤價838已突破布林上軌834.91」）。
- 禁止「應該會漲、值得期待」等主觀樂觀詞；沒訊號就如實說「數據不足以支持判斷」。
- 必須同時列出支持與反對進場的客觀證據，不能只挑對決策有利的講。

### 跟直接貼文字的差異
- 這種自行分析出來的卡片，`raw` 欄位不會有使用者貼的原文，改成放我自己整理的原始數據摘要（列出讀了哪些指標、哪些數字），
  讓使用者可以回頭檢查我的判斷依據。
- 勝率評估是模型自行估算的數字（沒有固定公式可還原），不用假裝跟 Gemini 算出來的百分比精確一致，方向與邏輯對齊即可。

## 其他檔案

- `.gitignore` 只允許 `.gitignore` / `index.html` / `style.css` / `data.js` / `render.js` / `CLAUDE.md` 進版控，
  這個資料夾裡其他既有的個股分析 html/png、python 程式（`台X資訊.py` 等）、快取、資料庫都刻意排除，只留在本機。
- 卡片正面欄位對應的顏色系統見 `style.css` 的 `.sec-*` / `.deco-*` class；如果要調整視覺風格，先讀過現有 CSS 的命名慣例再改，不要另起一套。
